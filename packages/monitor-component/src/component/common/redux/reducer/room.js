import common from './common.js';

export const lessonProgress = common('lessonProgress', '');
export const studentsMap = common('studentsMap', null);
export const userInfo = common('userInfo', {});
export const lessonInfo = common('lessonInfo', {});
export const isZmlExaming = common('isZmlExaming', false);
export const zmlCoverEleList = common('zmlCoverEleList', []);
export const initCourseware = common('initCourseware', {});
export const monitorParams = common('monitorParams', {});
