import Base from './base';

export default class StundentDrawTool extends Base {
  // constructor(dom, signalType) {
  //   super(dom, signalType);
  // }
  // handleSwitchSlide(pageNum, pageId) {
  //   this.showLayerById(pageId);
  // }
}